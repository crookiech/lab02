#ifndef MAIN_H 
#define MAIN_H 

#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
#include <algorithm>
#include <tuple>

auto createTuple(int n1, int n2, int n3, int n4) {}; // Функция создания кортежа с отдельными числами ip адреса
auto readingFile(std::string fileName) {}; // Функция считывания файла
void sortIPAdresses(std::vector<std::tuple<int, int, int, int>>& ipAddresses) {}; // Функция сортировки ip адресов

int main() {};

#endif